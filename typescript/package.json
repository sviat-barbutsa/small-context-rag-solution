{
  "name": "small-context-rag-solution-typescript",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "TypeScript demo for managing small-context RAG.",
  "scripts": {
    "build": "tsc",
    "demo": "tsc && node dist/index.js --question \"Why can RAG fail when the context budget is too small?\"",
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0"
  }
}
